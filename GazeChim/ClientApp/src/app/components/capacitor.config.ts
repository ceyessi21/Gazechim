import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gazechim.app',
  appName: 'Gazechim',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
